<style>
    /* Estilo do fundo da página */
    body {
        background-image: url('/images/agenda-366244_960_720.jpg');
        background-size: cover;
        background-repeat: no-repeat;
        background-position: center;
        background-attachment: fixed;
        font-family: Arial, sans-serif;
        margin: 0;
    }

    /* Container principal centralizado com fundo preto translúcido */
    .container-home {
        max-width: 800px;
        margin: 50px auto;
        padding: 30px;
        text-align: center;
        border-radius: 10px;
        background-color: rgba(0, 0, 0, 0.6);
        box-shadow: 0 4px 8px rgba(0,0,0,0.5);
        color: white; /* Cor padrão do texto dentro do container */
    }

    /* Classe para aplicar texto preto em elementos específicos */
    .texto-preto {
        color: black !important;
    }

    /* Botões principais com espaçamento e cantos arredondados */
    .home-button {
        display: inline-block;
        padding: 12px 25px;
        margin: 10px;
        text-decoration: none;
        color: white;
        border-radius: 5px;
        transition: background-color 0.3s;
    }

    /* Botão azul para Serviços */
    .btn-services {
        background-color: #007bff;
    }

        .btn-services:hover {
            background-color: #0056b3;
        }

    /* Botão verde para Agendamentos */
    .btn-appointments {
        background-color: #28a745;
    }

        .btn-appointments:hover {
            background-color: #1e7e34;
        }

    /* Link de documentação com estilo diferenciado */
    .doc-link {
        display: block;
        margin-top: 20px;
        color: #555;
        text-decoration: underline;
    }
</style>

<div class="container-home">
    <h1>Bem-vindo ao AgendaPRO</h1>
    <p class="texto-preto">Escolha uma das opções abaixo:</p>

    <a href="@Url.Action("Index", "Services")" class="home-button btn-services">Ver Serviços</a>
    <a href="@Url.Action("Create", "Appointments")" class="home-button btn-appointments">Criar Agendamento</a>

    <a class="doc-link" href="https://learn.microsoft.com/aspnet/core" target="_blank" rel="noopener noreferrer">
        Documentação ASP.NET Core
    </a>
</div>
